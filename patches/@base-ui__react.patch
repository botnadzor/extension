diff --git a/internals/useAnimationsFinished.js b/internals/useAnimationsFinished.js
--- a/internals/useAnimationsFinished.js
+++ b/internals/useAnimationsFinished.js
@@ -58,6 +58,8 @@ function useAnimationsFinished(elementOrRef, waitForStartingStyleRemoved = false
     frame.cancel();
     const element = (0, _resolveRef.resolveRef)(elementOrRef);
     if (element == null) {
+      // A cleared ref means the transition has completed; skipping cleanup leaves popups mounted.
+      fnToExecute();
       return;
     }
     const resolvedElement = element;
diff --git a/internals/useAnimationsFinished.mjs b/internals/useAnimationsFinished.mjs
--- a/internals/useAnimationsFinished.mjs
+++ b/internals/useAnimationsFinished.mjs
@@ -52,6 +52,8 @@ export function useAnimationsFinished(elementOrRef, waitForStartingStyleRemoved
     frame.cancel();
     const element = resolveRef(elementOrRef);
     if (element == null) {
+      // A cleared ref means the transition has completed; skipping cleanup leaves popups mounted.
+      fnToExecute();
       return;
     }
     const resolvedElement = element;
